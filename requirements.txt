# The crawler itself uses only the Python standard library.
# Install these when a verified HTML source needs CSS selector support.
requests>=2.31,<3
beautifulsoup4>=4.12,<5

# Week-one data processing, visualization, and modeling.
pandas>=2.2,<3
pyarrow>=16,<20
jieba>=0.42,<1
matplotlib>=3.8,<4
seaborn>=0.13,<1
scikit-learn>=1.5,<2
xgboost>=2.1,<3
joblib>=1.4,<2
openpyxl>=3.1,<4

# Week-two Agent: real OpenAI-compatible Responses API mode.
openai>=1.40,<2

# Week-three integration and local demo UI.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
streamlit>=1.38,<2

# Week-two semantic RAG experiment: local FAISS vector store and Chinese embeddings.
faiss-cpu==1.13.0
sentence-transformers==3.4.1
torch>=2.8,<2.9
